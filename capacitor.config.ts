import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pingo_frontend_ang',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
